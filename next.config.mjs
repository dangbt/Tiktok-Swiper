/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['backend.iscore.club', 'picsum.photos', 'isport-india.s3.ap-south-1.amazonaws.com']
  }
};

export default nextConfig;
